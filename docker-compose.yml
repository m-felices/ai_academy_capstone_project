
services:
  streamlit:
    build: .
    container_name: agentic-rag-app
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped